// Tree indentation width (px)
export const TREE_INDENT_WIDTH = 20

// Drag preview indentation width (px)
export const DRAG_PREVIEW_INDENT_WIDTH = 16

// Drop zone detection thresholds
export const DROP_ZONE_COLUMN_RATIO = 0.75 // Right 25% triggers column-end drop
export const DROP_POSITION_Y_RATIO = 0.5 // Upper half = inside, lower half = below

// Drag preview limits
export const MAX_DRAG_PREVIEW_CHILDREN = 8

// Default settings
export const DEFAULT_SHOW_COLUMNS = 4
export const DEFAULT_EXPAND_DEEP = 4
